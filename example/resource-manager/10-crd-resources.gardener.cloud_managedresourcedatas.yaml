---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  labels:
    gardener.cloud/deletion-protected: "true"
  name: managedresourcedatas.resources.gardener.cloud
spec:
  group: resources.gardener.cloud
  names:
    kind: ManagedResourceData
    listKind: ManagedResourceDataList
    plural: managedresourcedatas
    shortNames:
    - mrd
    singular: managedresourcedata
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: creation timestamp
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ManagedResourceData holds non-sensitive serialized Kubernetes
          manifests referenced by a ManagedResource via spec.dataRefs.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          data:
            additionalProperties:
              format: byte
              type: string
            description: Data contains the serialized Kubernetes resource manifests.
              Keys are filenames and values are brotli-compressed YAML.
            type: object
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
